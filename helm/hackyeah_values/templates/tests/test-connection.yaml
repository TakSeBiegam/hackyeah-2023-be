apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hackyeah-be.fullname" . }}-test-connection"
  labels:
    {{- include "hackyeah-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hackyeah-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
